{
  "name": "crickettournament",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cds watch"
  },
  "dependencies": {
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@sap/cds": "^8.9.4",
    "@sap/cds-dk": "^8.9.4"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "sqlite::memory:"
        }
      },
      "authentication": {
        "kind": "mocked"
      }
    }
  }
}
